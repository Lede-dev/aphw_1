<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 타이틀 글자 색 -->
    <attr format="color" name="titleColor"/>

    <!-- 배경 색 -->
    <attr format="color" name="backgroundColor"/>

    <!-- 달력 배경 색 -->
    <attr format="color" name="calendarBackgroundColor"/>

    <!-- 주 표시 바 색 -->
    <attr format="color" name="weekBarColor"/>

    <!-- 달력의 일반적인 날짜 표시 색 -->
    <attr format="color" name="dayCharColor"/>

    <!-- 달력의 일요일/공휴일 표시 색 -->
    <attr format="color" name="sundayCharColor"/>

    <!-- 달력의 토요일 표시 색 -->
    <attr format="color" name="saturdayCharColor"/>

    <!-- 달력의 그리드 내부 색 -->
    <attr format="color" name="calendarViewColor"/>

    <!-- 달력의 그리드 격자 테두리 색 -->
    <attr format="color" name="calendarGridColor"/>

    <!-- 달력의 주간 뷰 요일/시간 색 -->
    <attr format="color" name="calendarTimeColor"/>

</resources>